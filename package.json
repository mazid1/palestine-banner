{
	"name": "palestine-banner",
	"version": "1.0.0",
	"description": "A simple, customizable banner to show support for Palestine",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "bun run clear && bun build ./src/index.ts --outfile ./dist/index.js --minify && tsc --emitDeclarationOnly --outDir dist",
		"dev": "bun run clear && bun build ./src/index.ts --outfile ./dist/index.js --watch & tsc --emitDeclarationOnly --outDir dist --watch",
		"clear": "rm -rf dist"
	},
	"keywords": ["palestine", "banner", "solidarity"],
	"author": "Mazedul Islam",
	"license": "MIT",
	"devDependencies": {
		"bun-types": "latest",
		"typescript": "^5.0.0"
	}
}
